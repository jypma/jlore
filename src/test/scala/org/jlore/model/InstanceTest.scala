package org.jlore.model

import org.scalatest.junit.AssertionsForJUnit
import org.junit.Assert._
import org.junit.Test

class InstanceTest extends AssertionsForJUnit {/*
  @Test def test {
    var i = new Instance.Create(ID.random).result
    var p = new Property.Create(ID.random).result
    p = new Property.SetName(p, Value("MyProp")).result
    i = new Instance.SetProperty(i, p, Some(Value("MyValue"))).result
    println (i.values)
  }*/
}